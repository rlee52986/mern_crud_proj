import { Component } from 'react';
import axios from 'axios';

export default class CreateMusician extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div></div>
        )
    }
};